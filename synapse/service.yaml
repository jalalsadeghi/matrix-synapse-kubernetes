apiVersion: v1
kind: Service
metadata:
  name: synapse-service
  namespace: synapse
  labels:
    app: synapse
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: synapse
  type: LoadBalancer
  # clusterIP: None